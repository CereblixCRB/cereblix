@AGENTS.md

This repo's agent instructions live in `AGENTS.md` (the open standard Codex reads
natively); the `@AGENTS.md` import above pulls them in so Claude Code follows the
same shared rules. Edit `AGENTS.md`, not this file.
